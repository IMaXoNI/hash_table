# Hash Table in C

This project is a small implementation of a hash table in C.

The hash table stores string keys and string values. Collisions are handled with doubly linked lists.

## Build and run tests

```bash
make run
```

## Check with Valgrind

```bash
make valgrind
```

## Notes

The project was written to demonstrate my practical C programming skills.