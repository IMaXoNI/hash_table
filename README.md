# hash table with collision handling
Just a simple implementation of a hash table with collision handling by a doubly linked list to show what I can do.

## run for test

```bash
make run
make valgrind
```